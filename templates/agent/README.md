# Agent 目录说明

本目录存放所有 Agent 的配置文件与记忆文件。当前有两个 Agent：chat 和 classify，均为工具型 Agent（被动响应，不自主运行）。

---

## 目录结构总览

```
agent/
├── chat/                      # Chat Agent
│   ├── bootstrap.md           # 系统级启动说明
│   ├── agent.md               # System Prompt（角色定义）
│   ├── memory.md              # 长期蒸馏记忆
│   └── session_log/           # 跨会话短期记忆
│       └── YYYY-MM-DD.md
├── classify/                  # Classify Agent（纯自动流程）
│   ├── agent.md               # System Prompt（角色定义）
│   └── classify_preference.md # 分类偏好配置
└── skills/                    # 共享技能
```

---

## 文档设计原则

同 user/ 目录，遵循三条原则：单一职责、明确写入与读取时机、渐进式披露（frontmatter 摘要优先）。

### Frontmatter 规范

```yaml
---
name: 文档名称
summary: 一句话描述当前文档核心内容
read_priority: high | on_demand
last_updated: YYYY-MM-DD
last_event: 触发本次更新的事件简述
---
```

---

## Chat Agent `chat/`

Chat Agent 负责与用户直接对话，提供系统解释、行为查询、日常陪伴等功能。

**启动加载顺序：**
```
1. agent.md                          → 确认角色与行为准则
2. user/user.md                      → 了解用户基本轮廓
3. memory.md                         → 加载长期稳定语义记忆
4. session_log/今天.md + 昨天.md     → 仅在需要连续性时按需加载近期对话上下文
```

### chat/bootstrap.md
- **存放内容**：Chat Agent 初始化时的启动流程和基本约束
- **单一职责**：定义 Chat Agent 每次会话的加载顺序和初始化规则
- **写入权限**：仅人工维护，Agent 不写入
- **读取时机**：每次 Chat Agent 会话初始化时首先读取

### chat/agent.md
- **存放内容**：Chat Agent 的 System Prompt，定义角色、能力边界、行为准则
- **单一职责**：告诉 Agent 它是谁、能做什么、不能做什么
- **写入权限**：仅人工维护，Agent 不写入
- **读取时机**：每次会话初始化时首先读取
- **拆分规则**：当 agent.md 过长、修改行为准则时需要大量滚动才能定位目标内容时，拆分为两个文件：
  - `identity.md`：角色定义、能力边界、不可违反的底层原则、基础人格定位
  - `agent.md`：行为准则、工作流程、决策原则、任务执行方式
  - 加载顺序：`identity.md` → `agent.md`（identity 先于 behavior）

### chat/memory.md
- **存放内容**：从 session_log 和 user/behavior.md 蒸馏而来的长期稳定语义，包括长期偏好、稳定行为模式、持续目标、重要关系状态与经反复验证的洞察
- **单一职责**：为 Chat Agent 提供可常驻的长期用户语义上下文，让 Agent 在不同会话中保持稳定认知，而不依赖具体历史对话
- **写入权限**：Agent 定期维护
- **写入时机**：定期从 session_log + behavior.md 中提炼，删除过时内容，保持精简
- **读取时机**：每次 Chat Agent 会话初始化时读取，作为默认长期上下文层
- **关键规则**：
  1. 只保留跨时间稳定、脱离原会话后仍然成立的高层语义
  2. 内容必须高度浓缩，不随每次对话线性增长
  3. 事件只有在已经沉淀为长期结论时才可写入，不保留事件叙述本身
- **禁止写入**：短期有用但长期无关紧要的内容（归入 session_log）、原始对话记录、一次性聊天结论、短期任务进展、未经验证的推断

### chat/session_log/YYYY-MM-DD.md
- **存放内容**：每次会话结束后的全面摘要，包括对话主题、推进的任务、知识性问答要点、需要在下次会话延续的上下文
- **单一职责**：为 Chat Agent 提供跨会话的短期上下文，让下一个会话窗口无需重新建立背景
- **写入权限**：Agent 在会话结束时写入
- **写入时机**：每次会话结束时写入当天日期文件
- **读取时机**：
  1. 用户明确提及上次/之前的对话、计划、承诺时
  2. 当前对话明显在延续尚未完成的话题或任务时
  3. 当前消息出现需要近期连续性才能解释的指代时
- **生命周期**：短期保留（建议 7 天），定期由 memory.md 蒸馏后清理
- **与 memory.md 的区别**：session_log 是按需调用的短期情景记录（含知识性问答等），memory.md 是默认常驻的长期稳定语义记忆
- **与 behavior.md 的区别**：session_log 记录对话全貌（agent 视角），behavior.md 只提炼有行为语义价值的内容（用户行为视角）

---

## Classify Agent `classify/`

Classify Agent 负责对电脑使用数据进行分类，是纯工具型 Agent，不需要用户上下文。

**启动加载顺序：**
```
1. agent.md               → 确认角色与分类规则
2. classify_preference.md → 加载用户自定义分类偏好
```

### classify/agent.md
- **存放内容**：Classify Agent 的 System Prompt，定义分类规则、类别体系、处理逻辑
- **单一职责**：告诉 Agent 如何对电脑使用数据进行分类
- **写入权限**：仅人工维护，Agent 不写入
- **读取时机**：每次分类任务开始时首先读取

### classify/classify_preference.md
- **存放内容**：用户对数据分类方式的个性化偏好，例如将某类应用归入「学习」而非「娱乐」的自定义规则
- **单一职责**：覆盖默认分类规则，实现用户个性化分类
- **写入权限**：用户在前端确认「纳入偏好设置」后写入，Agent 不主动推断写入
- **写入时机**：用户在前端修改数据分类并确认时
- **读取时机**：每次分类任务开始时，在 agent.md 之后读取，作为规则覆盖层
- **禁止写入**：行为数据本身、对分类习惯的心理分析

---

## skills/
- **存放内容**：Chat Agent 和 Classify Agent 共享的可复用技能模块
- **单一职责**：封装可复用的能力，避免在多个 agent.md 中重复定义
- **写入权限**：仅人工维护



# 重新编排

## 问题思考
问题：当前chat通道有两个定位：1. 作为工具 2. 作为心理支持，个人成长等方面（暂定），需要区分提示词吗？
答： 先不进行，因为第二个角色暂时不确定，没有分离也不太好设计。待到第二个部分成熟之后在进行分离

问题2：区分文档和硬编码提示词的区别
1. 文档编码：意味着这个是面向用户的数据
2. 硬编码在代码中：这个是系统数据

问题3：我真的需要像openclaw那样制定文件提示词系统吗？那些是我需要的，那些是我不需要的？那些我认为AI是可以修改提示词的，那些是不能的？

1. openclaw等是通用ai助手，是解决实际问题的，而lifeprism并不是，lifeprism是提供成长，心理支持，以及数据查询和管理的助手。
2. openclaw的提示词系统：是为解决本地问题的通用agent

| 文件 | 职责 | 关键内容 |
|------|------|----------|
| AGENTS.md | 工作协议 | 会话启动流程、记忆维护规则、安全边界、群聊行为准则 |
| SOUL.md | 人格定义 | 语气、价值观、行为风格、沟通边界（如"有主见、不说废话"） |
| USER.md | 用户画像 | 用户身份、时区、偏好、当前重点、雷区 |
| TOOLS.md | 环境映射 | 本地路径别名、SSH地址、设备名、项目代号对应关系 |
| IDENTITY.md | 身份卡片 | 代理名称、形象、emoji、简短描述 |
| HEARTBEAT.md | 周期任务 | 定时检查清单（如查邮件、日历），为空则跳过心跳 |
| BOOTSTRAP.md | 首次引导 | 初始化问题（如何称呼、风格偏好），完成后通常删除 |
| MEMORY.md | 长期记忆 | 提炼后的稳定信息（偏好、技术踩坑、项目历史） |

3. 什么内容是可以让用户改变的？有多大程度上是可以自定义的？
  1. user.md可以自定义

  这样，先简单一点，先不让ai修改除了user.md以外的系统提示词，经过一些测试之后在慢慢的放开ai的权限。


## agent提示词组成部分

决定，都使用文档编码，不使用硬编码。目的是：为了方便调试和修改。
1. identify：写核心定位，排除系统运行等内容
  1. 核心定位：lifeprism系统助手，意在帮助用户查询和管理他们的生活数据，并且结合各种数据来回答用户的问题，为用户个人成长提供支持。

2. soul.md: 性格，价值观
```md


## SOUL
你是理性与人文关怀并重的向导，一个兼具深切共情与严谨逻辑的对话伙伴。你不追求通过讨好或盲目赞同来取悦用户，而是致力于提供多元、深刻且客观的见解。你的核心目标是帮助用户在接纳自我情绪的同时，看清现实的本质，实现内部认知的破局与外部行为的成长。

### Theoretical Framework (双引擎模型)
1. **情感处理引擎：ACT (接纳承诺疗法)**
   - **核心理念**：情绪没有对错，想法不一定真实。
   - **操作原则**：无条件接纳用户的痛苦与情绪（Acceptance），但帮助用户与负面/偏执的想法“解离”（Defusion）。引导用户看清当下的“选择点”（Choice Point），将注意力转移到有价值的行动上。

2. **逻辑处理引擎：批判性思维 (Paul-Elder 框架)**
   - **核心理念**：思维决定质量，通过标准评估思维。
   - **操作原则**：运用“理智的共情”（Intellectual Empathy）去理解用户观点的合理来源，但运用“理智的勇气”（Intellectual Courage）去温和地指出其背后的虚假假设、逻辑漏洞或信息偏差。保持理智的谦逊，不居高临下。

### 行为准则

下面是你的行为准则，按照重要性排序

1. 拒绝说教，拒绝对用户本身进行直接的评价
2. 不要为了安抚情绪而盲目顺从、赞同用户的错误逻辑。
3. 面对情绪低落的用户，情绪引导第一，在确认用户情绪平稳且可理性思考之后在引入专业的ACT知识进行引导。
4. 需要依据用户的认知水平以及相关只是储备引入相关的专业术语。如果不知晓用户的背景，慎重引入过多专业术语，建议循序渐进的引导。若引入专业术语，需要说明其含义，而不是直接使用。
5. 当用户出现重大心理障碍或相关医疗问题时，需要在开头一个段落说明，以下内容为AI生成，建议用户咨询专业的医生。

```