# UTC 时区迁移项目完整过程复盘

## 第一阶段：问题发现和决策编写

### 用户 Prompt 1
"现在数据同步我觉得可能文档还差些内容，先确认spec和flow覆盖了数据同步的全部内容，之后再编写一个已知限制docs\known-limitations和adr（如果有adr就不写），就是当前仅仅是通过update_at来判定的，原因是本地和云端是错开的，只有一个端能同时修改文件（云端只有agent能修改数据（这个还需要派出subagent进行二次确认，agent-only模式不会主动修改其他数据）），所以当前的update_at完全够用（前提是服务器时间和本地时间都采用同样的时区，这个很重要，如果服务器是海外服务器，这个可能就不对了，对了这个可能是一个bug，需要查看当前是否有配置配置本地时区，可能需要增加一个全局配置，前端和后端都需要使用这个时区的时间，这样即使服务器位置不一样至少时间还是一样的），你先整理我的需求"

**Claude 回应**：整理用户需求，识别出时区不一致是核心问题

### 用户 Prompt 2
调用 `/write-decisions` skill，说明原因："编写决策，转化为UTC时区，存储格式为ISO格式，这里说一下之前为什么使用本地时区，是因为数据同步的时候当时是手写的代码，而且为了调式方便和数据库时间查看并且考虑到当前这个只是一个本地的桌面应用，不需要那么复杂，而且当前对于这个最佳实践并不里了解等各种原因。这里不需要写具体的后续修改内容（后续在决定）"

**Claude 操作**：
1. 读取 decision-template、user-design-summary、ADR index
2. 创建完整的 ADR 文档（260+ 行）：
   - 问题界定：时区不一致导致 LWW 冲突解决失败、前端日期错位
   - 历史原因：调试方便、桌面应用假设、知识局限
   - 方案对比：保持现状 vs UTC迁移 vs 混合策略
   - 最终决策：UTC + ISO 8601
   - 决策原因：解决P0问题、符合最佳实践、支持未来扩展
3. 复制到项目级和全局级两个位置
4. 更新两个 index.md 和 user-design-summary.md

### 用户 Prompt 3
"/compact" 然后 "现在说明一下具体的修改，因为这个修改内容虽然机械简单，但是存在一下问题：1. 内容太多 2. 当前代码中尤其是格式，定时任务等很多内容可能对于格式和时区有着隐形的要求，这些在修改之前需要先调查清楚 3. 数据库迁移 .现在需要先讨论一下要完成这个修改具体需要做什么内容"

**Claude 回应**：尝试进入 plan mode 但被用户中断

### 用户 Prompt 4
"不进行修改，现在是讨论主要需要做的那几个方面的修改，让我了解真个修改的全貌"

**Claude 回应**：梳理了 6 个主要修改方面：
1. 代码层面的时间生成修改（后端 Python + 前端 TypeScript + 数据库 DEFAULT）
2. 隐性依赖排查（定时任务、时间格式解析、时间范围查询、前后端协议、日志调试）
3. 数据库历史数据迁移（迁移策略、迁移脚本、格式统一）
4. 验证和测试（功能验证、跨时区验证、回归测试）
5. 文档和规范更新

## 第二阶段：PRD 编写

### 用户 Prompt 5
调用 `/to-prd` skill："先编写prd，我的需求是按照时间的最佳实践进行，前端自动转化为Data对象（这个具体位置我不清楚，可能在api层，若你也不清楚通过iqs搜索确定最佳实践），后端采用UTC时区，ISO格式，说清楚需要做的哪些方面，排查哪些可能的问题"

**Claude 操作**：
1. 派发两个 Explore agents 并行调查：
   - Agent 1：调查前端时间处理流程
   - Agent 2：排查后端隐性时区依赖
2. 使用 iqs 搜索前端最佳实践
3. Agent 1 完成，发现：
   - 前端无统一响应拦截器
   - dateUtils.ts 已有正确工具函数但使用不广
   - 22+ 处违规使用 `.toISOString().split('T')[0]`
4. Agent 2 返回错误结果（身份验证问题）
5. 直接用 Grep 补充排查后端代码模式
6. 编写完整 PRD（295 行），包含：
   - Problem Statement
   - Solution
   - 24 条 User Stories
   - 6 个 Implementation Decisions（后端代码/前端代码/数据库迁移/隐性依赖/测试/文档）
   - Testing Decisions
   - Out of Scope

### 用户 Prompt 6
创建分支："现在提交所有修改，然后创建分支（不用创建worktree）"

**Claude 操作**：
1. 创建并切换到分支 `feature/utc-timezone-migration`
2. 提交所有文档修改（commit `565aa8b`）

## 第三阶段：Issue 切分

### 用户 Prompt 7
调用 `/to-issues` skill："这个issue切分需要细致，因为修改内容特别多，而且每个模块可能都涉及，所以具体的修改需要按照模块来分（小模块（server，repo等），颗粒度不是前后端）。在开始之前首先需要对前后端的隐形要求进行排查，产出文档，后续具体负责这个模块的agent在阅读这个文档进行修改。"

**Claude 操作**：
1. 阅读 PRD
2. 提出初步切分方案（18 个 issues）：
   - Phase 0: Issue #1 排查隐性依赖（HITL）
   - Phase 1: Issue #2-4 基础设施
   - Phase 2: Issue #5-8 Server Services 层
   - Phase 3: Issue #9 API 层
   - Phase 4: Issue #10-11 LLM 和 Monitor 模块
   - Phase 5: Issue #12-15 前端模块
   - Phase 6: Issue #16 数据库迁移（HITL）
   - Phase 7: Issue #17-18 测试和文档

### 用户 Prompt 8
"排查前后端隐性时区依赖并产出指导文档，这个只是排查这些内容吗，我决定你的这个提示词可能会限制可能有的问题，不应该说只排查这些"

**Claude 回应**：修改 Issue #1 为开放式排查，不预设范围

### 用户 Prompt 9
"还有这个issue不用人工确认，改为AKF，只有一种情况下需要暂停任务，就算这个调研判断，当前如果采用UTC和ISO格式会给当前项目造成巨大的问题时停止。还有这个issue产出的内容还需要包括由于这个UTC的修改导致的其他功能的失效（只记录，不修改）"

**Claude 操作**：修改 Issue #1：
- Type 改为 AFK
- 添加停止条件（发现重大问题时停止）
- 产出文档增加"功能失效清单"

### 用户 Prompt 10
"可以"（确认切分方案）

**Claude 操作**：发布所有 19 个 issues（新增 Issue #19 后变为 19 个）

### 用户 Prompt 11
"每个issue前面都添加一句话，使用tdd skill完成任务"

**Claude 操作**：批量在所有 issue 文件开头添加"使用 tdd skill 完成任务"

### 用户 Prompt 12
"提交文档"

**Claude 操作**：提交所有 19 个 issues（commit `186e994`）

## 第四阶段：补充说明

### 用户 Prompt 13
"数据库测试我需要做说明，当前"D:\desktop\软件开发\LifeWatch-AI\localData\dataset\lifewatch_ai - utc-refactor.db"这个的作为备份的数据库，"D:\desktop\软件开发\LifeWatch-AI\localData\dataset\lifewatch_ai.db"这个是直接进行迁移测试的数据库，每次测试完成之后需要重置数据库，并且迁移测试应该分为2阶段，一个是单元测试，先确保代码的正确性，然后在进行数据迁移测试。"

**Claude 操作**：更新 Issue #16：
- 明确测试数据库环境（备份库 vs 测试库）
- 添加两阶段测试流程（单元测试 → 数据迁移测试）
- 更新 Acceptance criteria 分为两个阶段

### 用户 Prompt 14
"这个也不需要人工审查，需要在这个issue完成之后做对比即可（需要另一个agent来完成），这里直接在加一个issue比较简单，用来审核#16的完成情况"

**Claude 操作**：
1. 新增 Issue #19：数据迁移结果审核和对比验证
   - Type: AFK（自动对比验证）
   - 职责：对比迁移前后数据，验证正确性，产出审核报告
2. 修改 Issue #16：移除"需要人工审查"
3. 修改 Issue #17：依赖 #16 和 #19

### 用户 Prompt 15
"帮我编写一个prompt，我现在需要给一个agent，1. 让他先阅读需要阅读的文档 了解背景 2. 排除subagent完成整个迁移任务，他自己本身不做任何代码工作"

**Claude 回应**：编写协调 agent prompt，包含：
- 必读文档清单
- 执行顺序（7 个阶段）
- 派发策略（依赖关系、并行执行）
- 职责说明（派发任务、监控进度、处理阻塞）

### 用户 Prompt 16
当前请求：复盘全过程，写入 `过程0.md`

## 关键成果

1. **ADR 文档**：`docs/adr/2026-07-12-migrate-to-utc-timezone.md`
2. **PRD 文档**：`.scratch/utc-timezone-migration/prd.md`
3. **19 个 Issues**：`.scratch/utc-timezone-migration/01-19-*.md`
4. **两次提交**：
   - `565aa8b`: 添加决策和文档
   - `186e994`: 切分为 19 个 issues
5. **分支**：`feature/utc-timezone-migration`

## 决策要点

1. **时区策略**：统一使用 UTC，解决 LWW 冲突、前端日期错位问题
2. **格式策略**：统一使用 ISO 8601，带时区标识
3. **切分策略**：按后端小模块（repository/services/api/llm/monitor）切分，而非前后端
4. **测试策略**：两阶段测试（单元测试 → 数据迁移测试），独立审核验证
5. **执行策略**：所有 issues 使用 TDD skill，通过协调 agent 派发 subagents

## 依赖关系

```
#1 (排查隐性依赖)
  ├─→ #2 (Repository 基础) → #3-11 (后端各模块) → #16 (数据迁移)
  └─→ #12 (前端工具) → #13-15 (前端各模块)
                                    ↓
                                  #19 (迁移审核)
                                    ↓
                                  #17 (集成测试)
                                    ↓
                                  #18 (文档更新)
```
