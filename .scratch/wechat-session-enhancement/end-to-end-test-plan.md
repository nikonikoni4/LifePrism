# 微信会话管理增强功能 - 端到端测试计划

## 测试目的

验证微信会话管理增强功能的所有特性是否正常工作。

## 前提条件

- LifeWatch-AI 系统已启动
- 微信渠道已配置并连接
- 系统中已有至少 3 个不同的会话历史

## 测试环境

- 测试日期：____________________
- 测试人员：____________________
- 系统版本：____________________

---

## 测试用例 1：数据层 - session_id 支持

### 目标
验证 ChatHistoryManager 正确保存和加载 session_id

### 步骤
1. 通过微信发送几条消息，创建新的会话
2. 等待定时任务 `process_session_message()` 执行（最多 2 小时）
3. 检查 `lifeprismData/user/daily_data/chat_history.json`

### 预期结果
- [ ] 文件中的新记录包含 `session_id` 字段
- [ ] `session_id` 的值是有效的会话 ID（格式如 `xxx-xxx-xxx`）
- [ ] 旧记录（如果有）没有 `session_id` 字段，系统正常运行

### 实际结果
```
记录示例：
{
  "timestamp": "2026-07-05T...",
  "content": "...",
  "session_id": "_______________"
}
```

**状态**：□ 通过  □ 失败  □ 未测试

---

## 测试用例 2：工具 - query_session_list

### 目标
验证会话列表查询工具正常工作

### 步骤
1. 通过微信发送："我想切换到之前的会话，但不记得是哪个了"
2. 观察 AI 的响应

### 预期结果
- [ ] AI 主动询问："上次讨论的内容是什么？时间范围是什么？"
- [ ] 回答"昨天的"后，AI 调用 `query_session_list` 工具
- [ ] AI 按**序号**列出结果（第1个、第2个、第3个...）
- [ ] 每个结果包含：session_id、最新总结、最后消息
- [ ] 列出结果后，AI **等待用户选择**（不自动切换）

### 实际结果
```
AI 回复内容：


```

**状态**：□ 通过  □ 失败  □ 未测试

**备注**：
- □ AI 是否主动询问
- □ AI 是否按序号列出
- □ AI 是否等待用户选择

---

## 测试用例 3：工具 - query_session_history

### 目标
验证会话历史预览工具正常工作

### 步骤
1. 在测试用例 2 的基础上，回复："我不确定，能看看第1个的具体对话吗？"
2. 观察 AI 的响应

### 预期结果
- [ ] AI 调用 `query_session_history` 工具
- [ ] AI 显示该会话的最近 N 轮对话（默认 10 轮）
- [ ] 只显示 user 和 assistant 消息（过滤掉 tool 消息）
- [ ] 消息按时间倒序（最新的在前）

### 实际结果
```
AI 显示的对话内容：


```

**状态**：□ 通过  □ 失败  □ 未测试

---

## 测试用例 4：AI 引导 - 用户选择"第X个"

### 目标
验证 AI 正确识别用户选择并生成切换指令

### 步骤
1. 在测试用例 2 列出结果后，回复："第2个"
2. 观察 AI 的响应

### 预期结果
- [ ] AI 识别出用户选择的是第2个
- [ ] AI 回复格式为：
  ```
  请复制下面的指令发送
  /continue <session_id>
  ```
- [ ] 不是只说 session_id，而是生成**完整的可复制指令**

### 实际结果
```
AI 回复内容：


```

**状态**：□ 通过  □ 失败  □ 未测试

---

## 测试用例 5：AI 引导 - 用户模糊描述

### 目标
验证 AI 能识别模糊描述并匹配正确的会话

### 步骤
1. 在测试用例 2 列出结果后，回复："应该是关于数据库的那个"
2. 观察 AI 的响应

### 预期结果
- [ ] AI 从列表中找到最匹配的项
- [ ] AI 生成完整的切换指令

### 实际结果
```
AI 回复内容：


```

**状态**：□ 通过  □ 失败  □ 未测试

---

## 测试用例 6：命令增强 - /continue

### 目标
验证 /continue 命令显示最后两轮对话

### 步骤
1. 发送：`/continue <一个已存在的session_id>`
2. 观察系统响应

### 预期结果
- [ ] 响应包含成功提示：`[SUCCESS] 继续会话 <session_id>`
- [ ] 响应包含最后两轮对话：
  ```
  最后两轮对话:
  user:
  <用户最后一条消息>
  
  A:
  <助手最后一条回复>
  ```
- [ ] 如果会话消息少于两轮，显示现有内容
- [ ] 如果 session_id 不存在，返回错误提示

### 实际结果
```
系统响应：


```

**状态**：□ 通过  □ 失败  □ 未测试

---

## 测试用例 7：命令增强 - /new

### 目标
验证 /new 命令提示如何恢复上一个会话

### 步骤
1. 在一个已有会话中，发送：`/new`
2. 观察系统响应

### 预期结果
- [ ] 响应包含成功提示：`[SUCCESS] 新建会话 <new_session_id> --- 可以开始新的聊天了！`
- [ ] 响应包含恢复提示：
  ```
  可以通过使用以下指令恢复上一个会话：
  /continue <old_session_id>
  ```
- [ ] 如果是首次创建会话（没有上一个），不显示恢复提示

### 实际结果
```
系统响应：


```

**状态**：□ 通过  □ 失败  □ 未测试

---

## 测试用例 8：边界情况 - 日期筛选

### 目标
验证 query_session_list 的日期筛选功能

### 步骤
1. 通过微信发送："我想看看今天的会话"
2. 观察 AI 是否使用日期参数调用工具

### 预期结果
- [ ] AI 理解"今天"并转换为日期格式（YYYY-MM-DD）
- [ ] AI 调用工具时传入 `date_filter` 参数
- [ ] 只返回指定日期的会话

### 实际结果
```
AI 回复内容：


```

**状态**：□ 通过  □ 失败  □ 未测试

---

## 测试用例 9：边界情况 - 空结果

### 目标
验证没有符合条件的会话时的处理

### 步骤
1. 通过微信发送："我想看看去年的会话"（假设去年没有会话）
2. 观察 AI 的响应

### 预期结果
- [ ] AI 调用工具返回空结果
- [ ] AI 友好地告知用户："没有找到符合条件的会话"
- [ ] AI 建议用户尝试其他时间范围或描述

### 实际结果
```
AI 回复内容：


```

**状态**：□ 通过  □ 失败  □ 未测试

---

## 测试用例 10：兼容性 - 旧数据

### 目标
验证系统兼容没有 session_id 的旧数据

### 步骤
1. 手动编辑 `chat_history.json`，添加一条没有 `session_id` 字段的旧格式记录
2. 重启系统
3. 执行测试用例 2（查询会话列表）

### 预期结果
- [ ] 系统正常启动，没有报错
- [ ] 查询会话列表时，跳过旧格式记录
- [ ] 新格式记录正常显示

### 实际结果
```
系统行为：


```

**状态**：□ 通过  □ 失败  □ 未测试

---

## 测试总结

### 通过的测试用例数
____ / 10

### 失败的测试用例
```
编号    失败原因    严重程度（高/中/低）





```

### 需要修复的问题
1. 
2. 
3. 

### 提示词调整建议
如果 AI 行为不符合预期（如不按序号列出、不生成完整指令等），需要调整 `templates/agent/chat/tool.md` 的措辞。

建议调整的地方：
```




```

---

## 签名

测试人员：________________    日期：________________

审核人员：________________    日期：________________
