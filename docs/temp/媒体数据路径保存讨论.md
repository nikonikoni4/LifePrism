1. 当前MessageQueue的session_id完全由内部决定，也就是无法在外边传入一个不存在的session_id
2. 也就是说在使用wechat channel时无法将媒体文件和session数据放在一起
3. 但是有必要把媒体文件和session放在一起吗？没有
4. 媒体文件地址路径说明 ： 1. 使用settings.channel_path 来获取所有channel存放的位置 2.
  wechat的所有内容的存储位置是settings.channel_path / 'wechat' 3. 媒体数据路径 settings.channel / 'wechat' /
  'media' 4. 状态持久化数据保存在 settings.channel / 'wechat' / account.json
5. 后续需要做的内容（与lifeprism的agent对接），为session的message增加字段，需要保存没轮对话的媒体文件路径，下一次重复传入


<!-- 
其他修改：
1. send 的 session逻辑 修改了session的加载逻辑，如果session_id不存在（传入为None或传入的session_id存在，但是没有文件数据），都会创建一个新的session，而不是之前的报错
2. 这么修改的目的是在进行多轮对话的时候，可以在开始之前就决定该会话的id，不用第一次会话
不执行1的逻辑
第一次：bus -> send (无session_id) -> 创建一个新的session ->返回数据和session_id
需要获得session_id才能后续的对话
第二次：bus -> send (有session_id) -> 加载session ->返回数据
好像差别不大，算了放弃这个修改,作用不是那么大 -->