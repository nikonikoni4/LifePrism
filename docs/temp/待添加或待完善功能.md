## Waid窗口
当前在窗口内增加的任务会自动创建一个 To Do List，但在结束时会请求创建自定义时间块。时间块的内容记录应使用 To Do List 的名字，而非直接填写名称。完成操作后，点击开始、结束时，应弹出对话框用于编写这段时间的总结，而不是直接填写todolist。
情况：待完成

## 分类优化
当前的分类只是按照分类名称来进行的，但实际应该为名称增加描述，这样分类会更准确。

另外增加一个分类偏好设置，用于记录用户认为错误或不合理的内容，这样可以使下一次分类结合之前的偏好来进行
情况：待完成

## 截图分析优化
参考docs\temp\2026-05-20-截图分析问题讨论.md
情况：待完成

## ai优化
1. 当前及时在上下文中已经加载了user.md agent还是会调用工具去查看user.md，解决方法增加agent.md文档的规则 [已经完成]
2. 当前bootstrap.md如果存在，会执行bootstrap.md，但是，我设置的是每次系统启动就会从template中添加bootstrap，需要对这个bug进行修改 [已经完成]
3. 在合适的位置添加路径说明 [已经完成]
4. agent.md 等文档应该可以实现参数注入和参数校验，但是这几个prompt又比较特殊，因为这几个目前定义是agent可以自主修改的，不能使用集中的prompt管理。暂时没有想好agent是否有必要有修改这个的权限，而且每次所有的prompt都会从template覆盖，如果使用prompt管理还需要考虑修改机制，比较复杂。这样，还是决定先分离这些特殊的独立的文档，方便直接文件修改prompt和agent自主修改[已经完成]