l─────────────────────────────────────────────┐
│                                                                     │
│     ◀   ┌─────┬───────┬───────┬───────┬───────┐   ▶    [单栏][双栏]│
│         │ 目标 │ 计划书 │ 任务池 │  分配  │ 每日  │                 │
│         └─────┴───────┴───────┴───────┴───────┘                   │
│            ▲                                                       │
│            └── 当前选中的标签高亮                                    │
└─────────────────────────────────────────────────────────────────────┘
```

**胶囊导航规则**：
- 单栏模式：点击哪个标签显示哪个视图
- 双栏模式：
  - 点击"目标" → 显示组合1（目标+计划书）
  - 点击"计划书" → 显示组合2（计划书+任务池）
  - 点击"任务池" → 显示组合3（任务池+日历）
  - 点击"分配"或"每日" → 显示组合4（日历+每日任务）

**切换按钮** `◀` `▶`：
- 点击 `▶` 从组合1切换到组合2，再切换到组合3...
- 点击 `◀` 反向切换

---

### 视图状态联动

```
┌────────────────────────────────────────────────────────────────┐
│                        状态联动规则                             │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  目标列表 ──选择目标──▶ 计划书过滤为该目标的计划书               │
│                              │                                 │
│                              ▼                                 │
│  计划书编辑 ──点击同步──▶ 任务池更新，显示新解析的任务            │
│                              │                                 │
│                              ▼                                 │
│  任务池 ────拖拽任务────▶ 日历中该日期标记有任务                  │
│                              │                                 │
│                              ▼                                 │
│  日历 ────点击日期────▶ 每日任务显示该日详情                     │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

### 响应式适配

| 屏幕宽度 | 布局 |
|----------|------|
| > 1200px | 双栏布局，左右各50%宽度 |
| 768px - 1200px | 双栏布局，左40% 右60% |
| < 768px | 强制单栏布局 |

---

### 待定的界面问题

1. **目标卡片设计**：需要展示哪些信息？（名称、进度、投入时间、状态标签...）
2. **计划书编辑器**：使用现成的MD编辑器组件？还是自定义？
3. **日历组件**：周视图 vs 月视图？是否支持切换？
4. **拖拽体验**：拖拽时的视觉反馈、放置区域高亮等细节


### 前端代码架构设计

frontend/page/goals/
├── GoalPage.tsx                    # 主入口，只负责布局框架
├── components/
│   ├── layout/
│   │   ├── DualPaneLayout.tsx      # 双栏布局容器
│   │   ├── SinglePaneLayout.tsx    # 单栏布局容器
│   │   └── CapsuleNav.tsx          # 顶部胶囊导航
│   │
│   ├── views/                      # 各个独立视图（可单独使用）
│   │   ├── GoalListView/
│   │   │   ├── GoalListView.tsx
│   │   │   ├── mockData.ts
│   │   │   ├── types.ts            # 视图专用类型
│   │   │   └── components/
│   │   │       └── GoalCard.tsx
│   │   ├── PlanDocListView/
│   │   │   ├── PlanDocListView.tsx
│   │   │   ├── mockData.ts
│   │   │   ├── types.ts
│   │   │   └── components/
│   │   │       ├──PlanDocEditorView/
│   │   │       ├── PlanDocEditorView.tsx
│   │   │       ├── mockData.ts
│   │   │       ├── types.ts
│   │   │       └── components/
│   │   ├── TaskPoolView/
│   │   │   ├── TaskPoolView.tsx
│   │   │   ├── mockData.ts
│   │   │   ├── types.ts
│   │   │   └── components/
│   │   ├── CalendarView/
│   │   │   ├── CalendarView.tsx
│   │   │   ├── mockData.ts
│   │   │   ├── types.ts
│   │   │   └── components/
│   │   └── DailyTaskView/
│   │       ├── DailyTaskView.tsx
│   │       ├── mockData.ts
│   │       ├── types.ts
│   │       └── components/
│   │
│   ├── combinations/               # 视图组合（封装双栏联动逻辑）
│   │   ├── GoalPlanCombo.tsx       # 组合1: 目标 + 计划书
│   │   ├── PlanPoolCombo.tsx       # 组合2: 计划书 + 任务池
│   │   ├── PoolCalendarCombo.tsx   # 组合3: 任务池 + 日历（拖拽）
│   │   └── CalendarDailyCombo.tsx  # 组合4: 日历 + 每日任务
│   │
│   └── shared/                     # 共享层
│       ├── components/             # 共享组件
│       │   ├── TodoItem.tsx
│       │   ├── DraggableWrapper.tsx
│       │   └── DroppableArea.tsx
│       └── types/                  # 共享类型
│           ├── index.ts            # 统一导出
│           ├── common.ts           # 基础通用类型
│           └── entities.ts         # 跨视图共享的实体类型
│
├── hooks/
│   ├── useGoalStore.ts             # 目标状态管理
│   ├── usePlanDocStore.ts          # 计划书状态管理
│   ├── useTaskPoolStore.ts         # 任务池状态管理
│   ├── useDailyTaskStore.ts        # 每日任务状态管理
│   └── useDragDrop.ts              # 拖拽逻辑封装
│
└── context/
    └── GoalPageContext.tsx         # 页面级共享状态（选中的目标、日期等）


---

## 界面风格：极光生产力 (Aurora Productivity) - Redefined （todo相关内容除外）

### 1. 设计理念：Aurora Productivity（极光生产力）
**核心概念**：将 Bento Box Grid（苹果风格模块化） + Aurora UI（流动渐变） + Soft UI Evolution（柔和深度） 融合，打造一个既专业又富有生命力的目标管理界面。在设计UI和背景时增加一些毛玻璃质感

**设计哲学**：
- 不是冷冰冰的效率工具，而是有温度的成长伙伴
- 用流动的渐变暗示目标的动态演进
- 用模块化布局体现工作流的清晰结构
- 用柔和的深度感传递专注与沉浸

### 2. 风格定位
**美学方向**：Modern Warm Productivity（现代温暖生产力）

**灵感来源**：
- Linear 的清晰层次
- Notion 的模块化思维
- Apple VisionOS 的玻璃质感
- 北欧设计的温暖极简

**独特性宣言**：
> "告别千篇一律的蓝白灰，用温暖的极光渐变赋予目标管理生命力"

### 3. 配色方案
**主题：Warm Aurora（温暖极光）**

```text
┌─────────────────────────────────────────────────────────────────┐
│ 品牌主色 (Primary Gradient)                                      │
├─────────────────────────────────────────────────────────────────┤
│ • 极光渐变：#6366F1 → #8B5CF6 → #EC4899                         │
│   (靛蓝 → 紫罗兰 → 玫瑰粉)                                        │
│   用途：品牌标识、主要按钮、选中态高亮                              │
│                                                                 │
│ • 主色-靛蓝：#6366F1        - 主要交互色                         │
│ • 主色-紫罗兰：#8B5CF6      - 悬停/激活态                        │
│ • 主色-玫瑰：#EC4899        - 强调/焦点                          │
│                                                                 │
│ 背景色系 (Background)                                            │
├─────────────────────────────────────────────────────────────────┤
│ • 画布背景：#FAFAF9         - 温暖的米白色（非冷白）               │
│ • 卡片背景：#FFFFFF         - 纯白卡片                           │
│ • 次要背景：#F5F3FF         - 淡紫色调（呼应品牌色）               │
│ • 悬停背景：#EEF2FF         - 淡靛蓝色                           │
│                                                                 │
│ 文字色系 (Typography)                                            │
├─────────────────────────────────────────────────────────────────┤
│ • 标题文字：#1E1B4B         - 深靛蓝（非纯黑，更柔和）             │
│ • 正文文字：#374151         - 深灰色                             │
│ • 次要文字：#6B7280         - 中灰色                             │
│ • 弱化文字：#9CA3AF         - 浅灰色                             │
│                                                                 │
│ 功能色 (Functional)                                              │
├─────────────────────────────────────────────────────────────────┤
│ • 完成/成功：#10B981        - 翡翠绿                             │
│ • 进行中：#6366F1           - 主色靛蓝                           │
│ • 待处理：#F59E0B           - 琥珀色                             │
│ • 搁置/警告：#EF4444        - 珊瑚红                             │
│ • 今日焦点：#EC4899         - 玫瑰粉（品牌色呼应）                 │
│                                                                 │
│ 状态语义色 (State Semantic)                                       │
├─────────────────────────────────────────────────────────────────┤
│ • Pool 任务：#DBEAFE (浅蓝背景) + #2563EB (蓝色边框)              │
│ • Scheduled 任务：#F3E8FF (浅紫背景) + #8B5CF6 (紫色边框)         │
│ • Completed 任务：#D1FAE5 (浅绿背景) + #10B981 (绿色边框)         │
│ • Shelved 任务：#F3F4F6 (浅灰背景) + #9CA3AF (灰色边框)           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4. 字体方案
**选择理由**：避免 Inter、Roboto 等过度使用的字体，选择有个性但仍保持专业可读性的组合。

**字体配对**：Tech Startup 风格变体
- **标题字体**：Plus Jakarta Sans
  - 特点：几何感强、现代、友好、专业
  - Google Fonts: [Plus Jakarta Sans](https://fonts.google.com/specimen/Plus+Jakarta+Sans)
- **正文字体**：DM Sans
  - 特点：高可读性、低视觉疲劳、几何但柔和
  - Google Fonts: [DM Sans](https://fonts.google.com/specimen/DM+Sans)
- **等宽字体（代码/数据）**：JetBrains Mono
  - 用于：任务ID、时间戳、代码块
  - Google Fonts: [JetBrains Mono](https://fonts.google.com/specimen/JetBrains+Mono)

**CSS 导入**：
```css
@import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&family=DM+Sans:wght@400;500;700&family=JetBrains+Mono:wght@400;500&display=swap');
```

**Tailwind 配置**：
```js
fontFamily: {
  heading: ['Plus Jakarta Sans', 'sans-serif'],
  body: ['DM Sans', 'sans-serif'],
  mono: ['JetBrains Mono', 'monospace']
}
```

**字号规范**：
- 页面主标题：28px / 36px (font-semibold)
- 区块标题：20px / 28px (font-semibold)
- 卡片标题：16px / 24px (font-medium)
- 正文：14px / 22px (font-normal)
- 辅助文字：12px / 18px (font-normal)
- 小标签：11px / 16px (font-medium)

### 5. 视觉规范

**1. 圆角系统（柔和但不过度）**
- `xs`: 6px   - 小标签、徽章
- `sm`: 8px   - 按钮、输入框
- `md`: 12px  - 卡片、弹窗
- `lg`: 16px  - 大卡片、面板
- `xl`: 20px  - 胶囊导航、特殊容器
- `full`: 9999px - 圆形按钮、头像

**2. 阴影系统（Soft UI Evolution 风格）**
```css
/* 柔和阴影 - 非硬边缘 */
--shadow-sm: 0 1px 2px rgba(99, 102, 241, 0.05),
              0 1px 3px rgba(0, 0, 0, 0.05);
--shadow-md: 0 4px 6px rgba(99, 102, 241, 0.07),
              0 2px 4px rgba(0, 0, 0, 0.04);
--shadow-lg: 0 10px 25px rgba(99, 102, 241, 0.1),
              0 4px 10px rgba(0, 0, 0, 0.05);
/* 悬停时的上浮阴影 */
--shadow-hover: 0 14px 30px rgba(99, 102, 241, 0.15),
                 0 6px 12px rgba(0, 0, 0, 0.06);
/* 内阴影 - 用于输入框聚焦 */
--shadow-inset: inset 0 2px 4px rgba(99, 102, 241, 0.1);
```

**3. 边框系统**
- 默认边框：`1px solid #E5E7EB`
- 悬停边框：`1px solid #C7D2FE` （品牌色淡化）
- 聚焦边框：`2px solid #6366F1` （品牌主色）
- 状态边框：`3px solid [状态色]`（左侧竖线标识）

**4. 间距系统（基于 4px）**
- `2xs`: 4px   - 紧密元素
- `xs`: 8px   - 小间距
- `sm`: 12px  - 标准小
- `md`: 16px  - 标准
- `lg`: 24px  - 区块内
- `xl`: 32px  - 区块间
- `2xl`: 48px - 大区块

### 6. 特色视觉元素

**1. 极光渐变背景（页面顶部装饰）**
```css
.aurora-bg {
  background: linear-gradient(
    135deg, 
    rgba(99, 102, 241, 0.08) 0%,
    rgba(139, 92, 246, 0.05) 50%,
    rgba(236, 72, 153, 0.03) 100%
  );
  /* 微妙的流动感，不干扰内容阅读 */
}
```

**2. 胶囊导航设计**
- 背景：白色 + 柔和阴影
- 胶囊选中态：极光渐变背景 + 白色文字
- 胶囊未选中：透明背景 + 深灰文字
- 过渡：200ms ease-out

**3. 任务卡片状态指示**
- 左侧 3px 彩色竖线标识状态
- 悬停时：卡片微微上浮（translateY -2px）+ 阴影加深
- 选中时：边框变为品牌色 + 淡紫色背景
- 拖拽时：透明度 0.9 + 大阴影 + 轻微旋转（2deg）

**4. 进度条设计**
```css
.progress-bar {
  background: linear-gradient(90deg, #6366F1, #8B5CF6, #10B981);
  border-radius: 9999px;
  height: 6px;
  /* 渐变表示从进行中到完成的过渡 */
}
```

### 7. 交互动效规范
**基础参数**：
- 过渡时长：150-200ms
- 缓动函数：`cubic-bezier(0.4, 0, 0.2, 1)`

**悬停效果**：
- 卡片：`translateY(-2px)` + `shadow-hover`
- 按钮：`brightness(1.05)` + `scale(1.02)`
- 链接：color transition + underline reveal

**点击反馈**：
- 按钮：`scale(0.98)` → `scale(1)`
- 卡片：轻微缩放反馈

**页面切换**：
- 淡入淡出：opacity 0 → 1
- 轻微上移：translateY(8px) → translateY(0)

**拖拽交互**：
- 拖起：`scale(1.02)` + `rotate(2deg)` + `shadow-lg`
- 放置区高亮：背景色变化 + 虚线边框
- 放下：弹性归位动画

**微交互**：
- 勾选完成：checkbox 打勾动画 + 文字划线动画
- 状态切换：平滑颜色过渡
- 数字变化：滚动数字动画

### 8. 组件设计示例

**胶囊导航栏**
```text
┌─────────────────────────────────────────────────────────────┐
│  背景：#FFFFFF                                               │
│  阴影：shadow-md                                             │
│  圆角：20px                                                  │
│  内边距：8px                                                 │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ◀  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐  ▶  │   │
│  │    │ 目标 │ │计划书│ │任务池│ │ 分配 │ │ 每日 │     │   │
│  │    └──────┘ └──────┘ └──────┘ └──────┘ └──────┘     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  选中态：background: linear-gradient(135deg, #6366F1, #8B5CF6) │
│         color: #FFFFFF                                      │
│         border-radius: 12px                                 │
│         padding: 8px 20px                                   │
└─────────────────────────────────────────────────────────────┘
```

**任务卡片**
```text
┌─────────────────────────────────────────────────────────────┐
│ ┃  ☐ 学习 React 基础                               🏷️ 2h   │
│ ┃     └─ 来源：React学习计划.md                            │
│ ┃                                                          │
│ ┃  状态竖线：3px solid #6366F1 (Pool)                       │
│ ┃  背景：#FFFFFF                                           │
│ ┃  边框：1px solid #E5E7EB                                 │
│ ┃  圆角：12px                                              │
│ ┃  阴影：shadow-sm                                         │
│ ┃                                                          │
│ ┃  悬停：                                                  │
│ ┃    - translateY(-2px)                                    │
│ ┃    - shadow: shadow-hover                                │
│ ┃    - border-color: #C7D2FE                               │
└─────────────────────────────────────────────────────────────┘
```

### 9. 深色模式预设（可选扩展）
**背景色系**：
- 画布背景：#0F0E17
- 卡片背景：#1A1925
- 次要背景：#252336

**文字色系**：
- 标题文字：#FFFFFE
- 正文文字：#A7A9BE
- 次要文字：#72757E

**品牌色**保留不变，但亮度可微调：
- 主色靛蓝：#818CF8 (亮一点)
- 主色紫罗兰：#A78BFA

### 10. 设计系统变量汇总

```css
:root {
  /* 品牌色 */
  --color-primary: #6366F1;
  --color-primary-hover: #818CF8;
  --color-accent: #8B5CF6;
  --color-highlight: #EC4899;
  
  /* 渐变 */
  --gradient-aurora: linear-gradient(135deg, #6366F1, #8B5CF6, #EC4899);
  --gradient-aurora-soft: linear-gradient(135deg, rgba(99,102,241,0.08), rgba(139,92,246,0.05), rgba(236,72,153,0.03));
  
  /* 背景 */
  --color-bg-canvas: #FAFAF9;
  --color-bg-card: #FFFFFF;
  --color-bg-secondary: #F5F3FF;
  --color-bg-hover: #EEF2FF;
  
  /* 文字 */
  --color-text-primary: #1E1B4B;
  --color-text-secondary: #374151;
  --color-text-muted: #6B7280;
  --color-text-subtle: #9CA3AF;
  
  /* 功能色 */
  --color-success: #10B981;
  --color-warning: #F59E0B;
  --color-error: #EF4444;
  --color-info: #6366F1;
  
  /* 边框 */
  --color-border: #E5E7EB;
  --color-border-hover: #C7D2FE;
  --color-border-focus: #6366F1;
  
  /* 圆角 */
  --radius-xs: 6px;
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 20px;
  
  /* 阴影 */
  --shadow-sm: 0 1px 2px rgba(99,102,241,0.05), 0 1px 3px rgba(0,0,0,0.05);
  --shadow-md: 0 4px 6px rgba(99,102,241,0.07), 0 2px 4px rgba(0,0,0,0.04);
  --shadow-lg: 0 10px 25px rgba(99,102,241,0.1), 0 4px 10px rgba(0,0,0,0.05);
  --shadow-hover: 0 14px 30px rgba(99,102,241,0.15), 0 6px 12px rgba(0,0,0,0.06);
  
  /* 过渡 */
  --transition-fast: 150ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-normal: 200ms cubic-bezier(0.4, 0, 0.2, 1);
  
  /* 字体 */
  --font-heading: 'Plus Jakarta Sans', sans-serif;
  --font-body: 'DM Sans', sans-serif;
  --font-mono: 'JetBrains Mono', monospace;
}
```

## 逻辑重构

### 核心架构：单一数据源 + 视图投影

```
┌─────────────────────────────────────────────────────────────────────┐
│                          Goal 目标                                   │
│  status: 'active' | 'completed' | 'shelved'                         │
│  commitment_id (可选)                                                │
│  plan_docs: List[PlanDoc]                                           │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       PlanDoc 计划书                                 │
│  存储位置: customData/plans/{goal_id}/{plan_name}.md                 │
│  状态: 'active' | 'completed' | 'shelved'                           │
│  功能: 思维缓冲区，从想法到执行的桥梁                                   │
│  格式要求: 包含 # 格式要求、# 草稿、# 正式 三个一级标题                  │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     TodoItem 统一任务模型                            │
│                    （数据库为单一数据源）                              │
│                                                                     │
│  ┌─────────────┐      ┌─────────────┐      ┌─────────────┐         │
│  │  任务池视图  │ ◄──► │ 每日Todo视图 │ ◄──► │  MD文档视图  │         │
│  │   (Pool)    │      │ (Scheduled) │      │ (同步回写)   │         │
│  └─────────────┘      └─────────────┘      └─────────────┘         │
└─────────────────────────────────────────────────────────────────────┘
```

---

### TodoItem 统一数据模型

不使用继承，所有Todo使用统一模型，通过字段区分用途：

```python
class TodoItem:
    # === 基础属性 ===
    id: int                              # 唯一ID
    content: str                         # 任务内容
    parent_id: Optional[int]             # 父任务ID（支持无限层级嵌套）
    goal_id: Optional[str]               # 关联的Goal ID
    plan_doc_id: Optional[int]           # 来源计划书ID
    
    # === 来源追踪（用于MD同步） ===
    source_type: str                     # 'manual' | 'plan_doc'
    source_anchor_id: Optional[str]      # MD中的锚点ID（格式: lp:xxxxxx）
    
    # === 状态管理 ===
    state: str                           # 'pool' | 'scheduled' | 'completed' | 'shelved'
    scheduled_date: Optional[str]        # 安排到某天的日期 (YYYY-MM-DD)
    
    # === 时间相关 ===
    expected_finish_at: Optional[str]    # 预计完成日期
    actual_finish_at: Optional[str]      # 实际完成日期
    delay_days: Optional[int]            # 拖延天数 (actual - expected)
    delay_reason: Optional[str]          # 拖延/未完成原因
    
    # === 显示属性 ===
    color: str = "#FFFFFF"
    order_index: int                     # 每日列表排序
    pool_order_index: Optional[int]      # 任务池排序
    
    # === 嵌套展示（非持久化） ===
    children: Optional[List["TodoItem"]] = None
```

---

### 状态流转设计

```
                         schedule (安排日期)
┌────────────┐  ────────────────────────────►  ┌─────────────┐
│    pool    │                                  │  scheduled  │
│  (任务池中) │  ◄────────────────────────────  │ (已安排日期) │
└────────────┘         unschedule (取消安排)    └─────────────┘
      │                                               │
      │ shelve                              complete  │
      ▼                                               ▼
┌────────────┐                              ┌─────────────┐
│   shelved  │                              │  completed  │
│   (搁置)   │                              │  (已完成)    │
└────────────┘                              └─────────────┘

关键规则：
1. 父任务状态变化时(scheduled/shelved)，子任务自动跟随
2. 父任务完成与子任务完成相互独立，无强制关联
3. 计划书搁置时，其下所有未完成Todo自动变为搁置状态
```

---

### 任务池视图展示规则

- 显示 `state='pool'` 的任务
- **也显示** `state='scheduled'` 的任务，但用**不同样式**区分（如半透明、标签等）
- 支持嵌套展示：拖动父任务时，子任务跟随

---

### MD文档同步机制

#### 锚点格式（HTML注释，渲染时不可见）

```markdown
- [ ] 学习React基础 <!-- lp:a1b2c3d4 -->
  - [ ] 完成官方教程 <!-- lp:e5f6g7h8 -->
  - [ ] 做一个小项目 <!-- lp:i9j0k1l2 -->
- [x] 阅读CSAPP <!-- lp:m3n4o5p6 -->
```

#### 同步时机

| 操作 | 行为 |
|------|------|
| **打开计划书页面** | 自动扫描MD，同步到数据库（新增/更新/检测orphan） |
| **关闭计划书页面** | 自动将数据库状态回写到MD（更新 [ ] ↔ [x]） |
| **编辑MD内容时** | 手动触发同步按钮 |

#### 扫描流程（MD → 数据库）

```
1. 解析MD中所有 - [ ] 和 - [x] 项
2. 检查是否有锚点ID：
   - 有锚点：根据锚点ID匹配数据库记录，更新内容
   - 无锚点：生成新锚点ID，创建新TodoItem，回写锚点到MD
3. 数据库中有锚点但MD中找不到：标记为 orphan（可选：提示用户处理）
```

#### 回写流程（数据库 → MD）

```
1. 读取整个MD文件内容
2. 遍历数据库中该计划书的所有Todo
3. 根据锚点ID定位MD中的位置
4. 更新 [ ] ↔ [x] 状态
5. 写回文件
```

---

### 数据库Schema变更

#### 新增：plan_doc 表

```sql
CREATE TABLE plan_doc (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,                    -- 计划书名称
    file_path TEXT NOT NULL,               -- MD文件相对路径
    goal_id TEXT,                          -- 关联的Goal ID
    state TEXT DEFAULT 'active',           -- 'active' | 'completed' | 'shelved'
    created_at TEXT,
    updated_at TEXT
);
```

#### 修改：todo_list 表（新增字段）

```sql
ALTER TABLE todo_list ADD COLUMN plan_doc_id INTEGER;      -- 来源计划书ID
ALTER TABLE todo_list ADD COLUMN source_type TEXT DEFAULT 'manual';  -- 'manual' | 'plan_doc'
ALTER TABLE todo_list ADD COLUMN source_anchor_id TEXT;    -- MD中的锚点ID
ALTER TABLE todo_list ADD COLUMN scheduled_date TEXT;      -- 安排日期
ALTER TABLE todo_list ADD COLUMN delay_days INTEGER;       -- 拖延天数
ALTER TABLE todo_list ADD COLUMN delay_reason TEXT;        -- 拖延/未完成原因
```

#### 修改：goal 表（新增字段）

```sql
ALTER TABLE goal ADD COLUMN commitment_id TEXT;            -- 关联的承诺ID（可选）
ALTER TABLE goal ADD COLUMN current_time_spent INTEGER DEFAULT 0;  -- 当前投入时间（分钟）
-- status 字段已存在，确保支持 'active' | 'completed' | 'shelved'
```

---

### 级联行为规则

| 触发 | 影响范围 | 行为 |
|------|----------|------|
| Goal 搁置 | Goal → PlanDoc → Todo | **待定**：是否级联搁置 |
| PlanDoc 搁置 | PlanDoc → Todo | 所有未完成Todo自动变为搁置 |
| 父Todo 安排日期 | 父Todo → 子Todo | 子任务跟随变为 scheduled 状态 |
| 父Todo 搁置 | 父Todo → 子Todo | 子任务跟随变为 shelved 状态 |
| 父Todo 完成 | 父Todo → 子Todo | **无影响**，子任务状态独立 |
| 子Todo 完成 | 子Todo → 父Todo | **无影响**，父任务状态独立 |

---

### 多层级任务展示规则

#### 设计原则

数据库采用**邻接表模型**（`parent_id` 指向父任务），多层级结构在**应用层构建**而非数据库递归查询。所有界面共享同一份 `TodoItem` 数据源，通过不同的过滤条件和渲染方式展示。

#### 三个界面的展示策略

| 界面 | 展示方式 | 查询条件 | 说明 | todo的UI类型 |
|------|----------|----------|------|------|
| **任务池** | 树形结构（可折叠） | `plan_doc_id = X` | 按计划书批量加载所有任务，内存中按 `parent_id` 构建树 | 普通（TodoItem） |
| **任务安排（日历拖拽）** | 平级结构，仅显示父任务 | `parent_id IS NULL AND state = 'pool'` | 子任务不显示，拖拽父任务时子任务自动跟随 | 普通（TodoItem） |
| **每日聚焦** | 树形结构（可折叠） | `scheduled_date = 'YYYY-MM-DD'` | 按日期批量加载，内存中按 `parent_id` 构建树 | 详细（TodoItemDetailed） | 

#### 数据流示意

```
┌─────────────────────────────────────────────────────────────────┐
│                    TodoItem 数据库（单一数据源）                  │
│                                                                 │
│  id | content | parent_id | state | scheduled_date | ...        │
└─────────────────────────────────────────────────────────────────┘
                              │
           ┌──────────────────┼──────────────────┐
           ▼                  ▼                  ▼
    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
    │   任务池     │    │  任务安排    │    │  每日聚焦   │
    │  (树形视图)  │    │ (平级+仅父)  │    │  (树形视图) │
    └─────────────┘    └─────────────┘    └─────────────┘
```

#### 拖拽时的级联更新逻辑

当用户将父任务从任务池拖拽到日历某一天时：

```sql
-- 父任务及其所有子任务同时更新
UPDATE todo_list 
SET scheduled_date = '2026-01-30', state = 'scheduled'
WHERE id = 父任务ID 
   OR parent_id = 父任务ID;
```

#### 前端树形构建流程

1. **一次性查询**：按 `plan_doc_id` 或 `scheduled_date` 批量获取扁平列表
2. **内存中构建**：遍历列表，按 `parent_id` 关系组装成树形结构
3. **递归渲染**：使用 `TodoItemTree` 组件递归展示多层级

---

### 待定/搁置的问题

以下问题在后续实现过程中根据实际需求再确定：

1. **从每日拖回任务池**：恢复原位 还是 添加到底部？
2. **Goal搁置级联**：是否级联搁置下属计划书和Todo？
3. **锚点前缀**：当前暂定 `lp:`，可根据需要调整
4. **缩进格式支持**：2空格、4空格、Tab 是否都支持？（建议都支持）
5. **Orphan处理**：数据库有但MD中删除的任务如何处理？


