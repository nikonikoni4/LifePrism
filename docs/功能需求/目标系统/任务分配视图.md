---
id: feat-goals-task-allocation-view
title: 任务分配/日历视图 
module: goals
status: Partially done
  - 前端界面交互等功能已经完备，缺少与后端api对接
priority: P1
dependencies: 
  - todo模块api
created: 2026-02-02
updated: 2026-02-02
---

## 界面

**注意**，保持当前界面不变和交互不变

## 数据结构

**核心数据结构**： 位置frontend\page\goalsV2\types\todo.ts
interface TodoItem {
    id: number;
    content: string;
    parentId: string | null;
    goalId: string | null;
    planDocId: string | null;
    sourceAnchorId: string | null;
    state: 'pool' | 'scheduled' | 'completed' | 'shelved';
    scheduledDate: string | null;
    expectedFinishAt: string | null;
    actualFinishAt: string | null;
    delayDays: number | null;
    delayReason: string | null;
    color: string;
    orderIndex: number;
    poolOrderIndex: number | null;
    children?: TodoItem[];
}

## api

**重要**：暂时没有设置api，因为当前的数据是来自任务池的数据，在任务池中会一次性请求所有的todo