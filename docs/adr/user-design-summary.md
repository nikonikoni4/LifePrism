# User Design Summary

## 目的

记录用户在重大设计决策中的偏好、判断方式和风险倾向，为后续 agent 自主决策提供参考，也为用户复盘决策方式提供材料。

## 决策行为观察

| 日期 | 关联决策 | 观察 | 可参考性 | 风险提醒 |
| ---- | -------- | ---- | -------- | -------- |
| 2026-07-22 | [为 AUTOINCREMENT 表新增 hash_id 字段](.\2026-07-22-add-hash-id-to-autoincrement-tables.md) | 用户在迁移方法选型上展现了"知识盲区暴露 + 接受更优方案"的判断模式：初始倾向删表重建是因为"简单且我能懂"，主动承认对 SQLite ALTER TABLE 限制不了解。在 AI 提出 ALTER + 回填 + CREATE UNIQUE INDEX 方案并说明优势（不丢数据、与 m012 一致）后，用户立即采纳。用户不因"自己初始的选择"而固执，愿意在了解技术细节后切换到更优方案。 | 适用于涉及数据库迁移、SQLite 限制等技术细节场景。用户会主动暴露知识盲区，并在 AI 补充技术对比后做务实判断。 | 用户对 SQLite ALTER TABLE 限制不了解，后续 agent 在涉及 schema 变更时应主动补充 SQLite 的技术限制，而非假设用户了解。用户初始选择"简单方案"的倾向在了解限制后会修正。 |
| 2026-07-22 | [hash_id 定位为同步专用标识](.\2026-07-22-hash-id-sync-only-identifier.md) | 用户在 hash_id 定位决策上展现了"风险控制驱动方案收缩"的判断模式：初始想用 hash_id 作主键（语义统一），但在 AI 评估具体改动面（5 张表 Provider + API + Service + 前端 + LLM Tool 全部需要改造、WHERE 条件静默失败）后，判断"修改面太广"，主动收缩到"hash_id 仅作同步专用标识"方案。用户从"理想方案"回到"务实方案"的关键转折点是"WHERE 命中 0 行 → 更新静默失败"这一具体风险的识别。 | 适用于涉及主键改造、调用链路影响评估的场景。用户会从"改动面是否可控"和"是否有静默失败风险"两个维度评估方案，而非仅看语义完备性。 | hash_id 不作主键的决策依赖"本地 CRUD 不需要用 hash_id"前提。PRD 3 完成后若发现本地删除也要写墓碑，需切换到方案 B（改 _PRIMARY_KEY）。后续 agent 在 PRD 3 实现时应主动验证此前提是否仍然成立。 |
| 2026-07-22 | [habit 链条表从 SYNC_TABLES 移除](.\2026-07-22-habit-chain-tables-not-synced.md) | 用户在 habit 链条表同步决策上展现了"需求驱动 + 前置条件显式化"的判断模式：(1) 否决"保留同步但文档化已知限制"的方案，理由是会留下用户可见 bug（链条错乱），与"修改不能影响正常运行"原则冲突；(2) 明确区分"临时移除"和"永久放弃"——给出恢复同步的前置条件（云端 agent 需要 + 服务器网页浏览），并强调恢复前必须先解决 chain_id 外键问题。用户的判断依据不是"PRD 阶段"而是"当前是否有实际需求"——当前云端 agent 无 habit 链条数据需求，因此不同步也能接受。 | 适用于涉及功能禁用、已知限制记录的场景。用户会从"当前是否有实际需求"出发评估必要性，而非从"未来可能需要"出发。对恢复条件会做显式标注（前置条件 + 必须先解决的问题）。 | "临时移除"依赖前提 2（云端 agent 无 habit 链条数据需求）。如果未来云端 agent 扩展了 habit 相关能力，需要重新评估。chain_id 外键问题是恢复同步的硬性前置条件，不能跳过。 |
| 2026-07-09 | [同步原子性策略](.\2026-07-09-sync-atomicity-strategy.md) | 用户在讨论同步错误处理粒度时，主动分析了 row-level best-effort 的致命缺陷——时间戳推进后失败行会永久丢失。用户判断依据是"整体的原子性才对"，认为 last_sync_time 应该代表"所有表已成功同步至此"的语义。 | 适用于涉及数据同步、增量查询、时间戳驱动重试的场景。用户会从"失败后能否恢复"的角度评估策略，而非仅看正常流程。 | 整体原子性会导致成功表重复同步（幂等但浪费资源）。如果同步表数量大幅增加或单次同步耗时长，可能需要重新评估"table-level 容错 + last_sync_time 整体原子"的优化方案。 |
| 2026-07-09 | [密钥存储策略](.\2026-07-09-key-fallback-strategy.md) | 用户在讨论密钥存储方式时，对 .env 和 config.yaml 的区别不确定，主动询问"写入 .env 还是写入文件，和直接写在配置里有什么区别"。用户的决策依据是"简单一点，全部写在一个文件里到时候读取方便"，而非对 12-factor app 实践的认知。 | 适用于用户对"标准实践"不执着、优先选择最简单方案的决策场景。用户会主动暴露自己的知识盲区并请求确认。 | 用户对 .env / 环境变量 / 12-factor app 的认知不深，决策更多基于"直觉上的简单"而非技术对比。后续 agent 在涉及"标准实践 vs 简单方案"时，应主动补充技术对比，而非假设用户了解标准实践的优势。如果未来迁移到容器化部署，需要重新评估。 |
| 2026-07-09 | [冲突解决策略](.\2026-07-09-lww-conflict-resolution.md) | 用户否决 CRDT 和版本号方案的核心依据是"表更改的内容太多"和"使用频率不高，存在较大时间差"。用户从实际使用场景出发判断冲突概率，而非从理论模型出发。2026-07-14 补充了三个可校验的决策前提——前提 1（主备模式）、前提 2（NTP 时钟偏差可控）、前提 3（主备切换间隔大于时钟偏差），并明确记录了每个前提失效时的备选切换路径。用户特别强调前提 3 的作用：即使云端时钟慢于本机，只要切换间隔覆盖偏差，LWW 的时间戳比较仍然正确。 | 适用于涉及冲突解决、并发控制等理论方案选择时，用户会从实际使用模式出发评估必要性，而非追求理论上的完备性。用户对"决策前提可校验"有明确要求——每条前提必须能在未来重新验证是否仍然成立。 | 这种"从使用场景倒推技术方案"的方式在 MVP 阶段是合理的，但如果使用模式变化（如多端同时编辑），需要重新评估。三个前提已将失效条件显式化（决策逻辑表），后续 agent 在修改同步相关代码前应先验证前提是否仍然成立，而非假设"LWW 永远是对的"。 |
| 2026-07-09 | [通信架构](.\2026-07-09-rest-polling-communication.md) | 用户选择 REST 轮询的决策依据与冲突解决策略一致——从实际使用模式出发判断"同步频率低、时间差大"，因此不需要 WebSocket 的实时性。 | 与冲突解决策略的观察一致，用户在技术方案选择时从使用场景出发，不追求技术先进性。 | 同上。 |
| 2026-07-09 | [cloud_init.yaml 初始化策略](.\2026-07-09-cloud-init-atomic-strategy.md) | 用户在配置初始化策略上选择"验证失败不删除"，核心关注点是"密钥不可恢复"——密钥由本地 keyring 生成，删除文件后如果本地程序已关闭则无法重新获取。 | 适用于涉及不可恢复资源（密钥、token 等）的处理策略。用户对"不可恢复"有清醒认知，倾向于保留而非删除。 | 无重大风险，"保留文件方便重试"是合理的防御性策略。 |
| 2026-07-12 | [迁移到 UTC 时区和 ISO 8601 格式](.\2026-07-12-migrate-to-utc-timezone.md) | 用户主动承认历史技术债——当初选择本地时区是基于"调试方便"、"桌面应用不需要那么复杂"、"对最佳实践不了解"等原因。在了解业界最佳实践（Laravel/ActivityWatch 等都强制 UTC）和现有问题（时区不一致导致数据同步失败）后，选择迁移到 UTC。用户对"即使桌面应用也需要 UTC"的认知转变来自具体案例（ActivityWatch）而非理论说教。 | 适用于用户对历史决策有清醒认知、愿意承认知识局限、并在有充分证据时修正方向的场景。用户的决策转变依赖于"具体案例 + 现有问题"的双重驱动，而非单纯的理论最佳实践。 | 迁移成本大（100+ 处代码 + 历史数据），但用户已接受。需注意用户对"最佳实践"的认可是基于"已出现问题 + 同类项目案例"，而非预防性采纳。后续 agent 在推荐最佳实践时，应结合"当前痛点 + 具体案例"，而非仅引用官方文档。 |
| 2026-07-12 | [时间转换职责分层架构](.\2026-07-12-time-conversion-layering.md) | 用户在决策过程中经历了明显的动摇：先是质疑"一层层过滤"是否合理，一度想改 Repository 层增加 `return_local_time` 参数（因为"修改点较多且容易出错"），又主动提出装饰器方案（想减少重复代码），并要求"派出 agent 审查是否合理"。审查报告指出装饰器的静默失败、字段用途差异等风险后，用户确认回到原方案。用户在动摇时会主动寻求外部审查，而非固执己见。 | 适用于用户对架构方案有动摇时，会主动提出替代方案并请求验证的场景。用户不盲从"审查报告说不行就不行"，而是基于具体风险点（字段用途差异、静默失败）做最终判断。 | 决策过程较长（4 次迭代），但最终方案与初始方案一致。后续 agent 在类似架构决策中，应允许用户充分讨论替代方案，即使最终回到原方案。装饰器方案的否决理由（字段用途差异、静默失败）具有通用性，后续涉及"自动转换"类方案时可参考。 |
| 2026-07-13 | [自定义字段时间不转换 + 系统级 datetime 字段](.\2026-07-13-custom-records-time-string-not-convert.md) | 用户在处理自定义记录模块的时区迁移问题时，明确区分了"系统时间"和"用户数据"两个概念。对自定义字段中的时间，用户选择原样存储不转换，理由是：(1) 自定义字段只用于显示，不做查询筛选；(2) YYYY-MM-DD HH:MM:SS 格式天然支持字典序排序；(3) 尝试识别并转换会有误转换风险。更关键的是，用户主动发现了一个更深层的设计问题——当前用 `created_at` 做日期筛选语义不正确（创建时间 ≠ 事件时间），并提出了新增必填系统级 datetime 字段的方向。用户在做时间相关决策时，不是简单套用"系统所有时间都转 UTC"的规则，而是根据字段的**职责定位**（系统基础设施 vs 用户数据）来差异化处理。 | 适用于需要区分"系统管理的数据"和"用户输入的数据"的场景。用户的判断模式：先分析字段的职责，再决定处理策略，而非一刀切。当 Agent 能提供合理默认值时，用户倾向必填约束（event_time 必填，无时间时填入当前时间）。 | 区分"系统字段"和"用户字段"是好的抽象，但需要确保边界清晰且在代码中有明确标记。如果未来自定义字段引入 `datetime` 类型并使用它做查询，这个边界会变得模糊，需要重新讨论转换策略。另外，"不转换"策略的前提是"不用于查询"——这是一个容易在需求演变中被忽视的前提，需要在新增查询功能时主动检查。 |
| 2026-07-13 | [日期到 UTC 转换边界与查询参数传递策略](.\2026-07-13-date-to-utc-conversion-boundary.md) | 用户对"就近转换"的位置有明确理解——"组件边界就是转换边界"，坚持"不能让本地时间干扰内部系统，让内部系统保持纯净"。在 AI 主张"API 层转换更便利"时，用户质疑"工程便利性"的实际收益，要求派 subagent 调查实际代码量。调查发现"只有 2 处转换，无工程复用性"后，用户确认采用组件内转换。在混合表决策时，用户提出"如果前端传错误 start_time，后端能否正确解析 date"的技术疑问，在确认可行后才选择"传 date + UTC 范围"方案。 | 适用于用户对"数据纯净性"有清晰原则的场景。用户会主动质疑 AI 提出的"便利性"理由，要求数据验证（实际代码量、实际复用度）而非理论分析。用户对技术方案的选择基于"前提成立"——明确记录决策前提和失效条件。 | 用户对"架构原则 vs 工程便利性"的权衡倾向于架构原则（14 行代码重复可接受）。但这个倾向是基于"复用性不存在"的前提，如果前提失效（转换需求超过 10 个 API），用户可能重新评估。后续 agent 应学习"记录决策前提和失效条件"的模式，而非仅记录最终决策。用户对"过度考虑"的判断依据是风险概率评估（前端格式由浏览器保证 + 格式化函数兜底），这种风险评估方式可参考。 |
| 2026-07-14 | [同步全量同步策略与 LWW 相等跳过](.\2026-07-14-sync-full-sync-strategy.md) | 用户在讨论全量同步方案时，最初提出"都做"（方案一+方案二），但在 AI 评估方案一的修改量、对当前逻辑的损坏、可能引起的 bug 后，用户主动收回，判断"方案二能够完全解决我现在的需求"、"方案一本身就有点过度设计了"。用户的决策依据是"务实优先"——当方案二能完全覆盖当前需求时，不引入方案一的复杂度。用户特别关注方案一在多客户端场景下的"静默数据丢失"风险，认为这种不可恢复的风险比方案二"依赖用户记忆"的风险更严重。在 LWW 相等跳过决策中，用户明确判断"update 相同应该跳过"，理由是无意义的覆盖操作应该避免。 | 适用于用户在"功能完备性 vs 务实简洁"之间的权衡场景。用户会从"当前需求是否完全覆盖"出发评估方案必要性，而非从"未来可能需要"出发。对"静默数据丢失"类风险特别敏感，优先级高于"依赖用户记忆"类风险。 | 用户对"都做"的初始倾向会在 AI 评估具体成本后修正。后续 agent 在用户提出"都做"时，应主动评估每个方案的修改量、对当前逻辑的损坏、可能引起的 bug，帮助用户做务实判断。用户对"过度设计"的判断依据是"当前需求是否完全覆盖"，而非"方案本身是否合理"。 |
| 2026-07-14 | [文件同步冲突处理方案](.\2026-07-14-file-sync-conflict-resolution.md) | 用户在文件同步方案设计中展现了系统性的决策方式：(1) 从不满足于"代码能跑"，会主动排查调用链路是否真正打通；(2) 参考外部成熟方案（思源笔记）但做务实简化——选择 per-file 而非完整 snapshot 树，因为主备模式下完整方案过度设计；(3) 白名单决策遵循"谁写谁同步"的原则——同步白名单 = Agent 工具白名单 + session，每个排除项都有明确的"前提失效时重评估"条件；(4) v2.0 中 MD 冲突从"用户手动处理"转变为"AI 自动合并"——用户识别到 user/ 下 MD 全由 AI 生成，AI 最理解内容语义，果断切换方案；(5) chat_history.json 从"保留在白名单内"变为"明确排除"——因为只有 dreaming task 会改它，而云端不启动 dreaming；(6) 所有决策都基于可校验的前提（主备模式、Agent 白名单不可绕过、云端无前端 API/无 dreaming、user/ MD 由 AI 生成），每个前提失效都有明确的备选触发路径。 | 用户对"决策前提可校验"的要求贯穿本次设计。更关键的是，用户在 v2.0 中展现了"反思与修正"能力——初始方案 MD 冲突由用户手动处理（因为假设 MD 是用户手写），但识别到"user/ MD 全由 AI 生成"后改为 AI 自动合并，并设计了完整的 CONFLICT_RESOLVE 消息类型。用户在做"谁来做这件事"的决策时，会基于"谁最理解这些数据"来判断——数据是 AI 生成的就交给 AI。后续 agent 应先问"这些数据是谁产生的"。此外，在白名单决策中，用户对 chat_history.json 的处理从"保留在目录级白名单里不做逐文件排除"改为"明确排除"，展示了在精确性上的持续完善——当排除理由足够坚固（写入者明确且只有一个）时，会做精确排除。 | 决策转变（手动→AI 自动）依赖前提 7（user/ MD 由 AI 生成）。如果用户开始直接编辑这些文件，AI 合并可能不符合期望，需恢复手动介入。AI 合并失败不做回退（保留本地版本，云端备份在 sync_conflict/），如果合并质量不稳定需评估是否引入确认步骤。chat_history.json 排除依赖前提 8（dreaming task 是唯一写入者+云端不启动 dreaming），如果云端未来启动 dreaming task 或新增其他写入者，需要重新评估。 |
| 2026-07-17 | [云端初始化与首次同步策略](.\2026-07-17-cloud-init-first-sync-full-clear.md) | 用户在否决两阶段方案（黑名单）并转向全清覆盖方案的过程中展现了关键判断：**从数据一致性目标反推方案有效性**——识破黑名单只能"局部治标"（过滤固定 ID 的种子数据），无法应对云端 agent 在首次同步前产生的噪音数据，明确指出"仅仅是黑名单并不能真正的确保数据一致性"。同时，用户通过**验证前提的真伪而非关注表面美观性**来否决环境音修复：追问 mood_impacts 自增键"是否有影响？"→ 验证无外键引用、mood_entries 存 name 非 ID → 确认无影响 → 判断不需要改动（即使与 TEXT ID 风格不一致）。这个判断模式可概括为"决策必须基于功能影响，而非风格一致性"。 |
| 2026-07-16 | [动态表同步定义对比方案](.\2026-07-16-dynamic-tables-sync-definition-comparison.md) | 用户在分析动态表同步 bug 时展现了精准的问题定位能力：(1) 不满足于表面现象（"每次触发重建"），主动追问根因——识别出"快照对比方向反了"+"兜底条件永真"两个独立逻辑问题；(2) 否决了 AI 提出的最小改动方案（持久化 last_synced_snapshot），理由是"无法覆盖本地新增动态表的场景"——坚持从根因方向问题入手而非治标；(3) 主动提出新增端点拉取云端定义做对比，坚持"对比要在 pull 之前"——因为 pull 是按表名拉取的，动态表不存在时无法作为拉取对象；(4) 在"建表是否写 meta 数据"决策上要求 AI 调研现有建表逻辑，基于"复用 generate_create_table_ddl 不需要 meta"和"避免 id 冲突"选择只执行 DDL 的方案；(5) 主动识别出"sync_once 期间需要禁止数据库修改"这个新的决策点，但明确判断"这偏离了当前 ADR 范围"，坚持决策边界不被扩大。 | 适用于涉及同步流程、建表逻辑、schema 与数据分离的场景。用户会主动识别"方向性错误"——不满足于修复表面现象，会追问"检测的方向和实际操作的方向是否一致"。用户对"决策范围"有清醒控制——识别到衍生问题时会明确标注为"需要独立决策"而非顺手处理，避免决策范围蔓延。 | 决策依赖前提 1（字段不会变）和前提 3（无并发，假设）。前提 1 失效场景已明确——前端或 Agent 支持修改字段时需重新决策。前提 3 是未验证的假设，如果 sync_once 期间有并发访问导致数据不一致（建表后到 pull 完成 meta 同步之间的窗口），需要单独决策并发锁机制。后续 agent 在修改 sync_once 流程时应先验证前提 3 是否仍然成立。 |
| 2026-07-17 | [文件冲突解决改用 diff3 + LLM 辅助合并](.\2026-07-17-conflict-resolution-diff3-replaces-llm.md) | 用户在 behavior.md 被 LLM 截断破坏（P0 生产 bug）后展现了完整的"事故驱动决策"模式：(1) 紧急修复后立即分析根因——识别出 LLM 自主合并 + 文件工具是风险根源，而非"prompt 写得不好"或"模型能力不足"；(2) 否决了"继续 LLM 自主合并但加 guard"的折中方案，理由是 LLM 不可控性无法通过工程手段完全消除——选择"LLM 降级为内容建议者、程序升级为决策执行者"的根本性责任重新划分；(3) 在 diff3 算法选型上否决了 merge3 包，理由是"开源协议纠纷可能性 + 自研够用"——与项目"无外部依赖"偏好一致，但用户是基于协议风险而非纯依赖偏好做判断；(4) 在串行 vs 一次调用决策上，用户基于"同文件修改后行号会变化"的工程细节选择串行（理解 B），体现了对实现细节的关注而非仅看架构层面。用户明确表示"LLM 进行文档冲突是可以的"——不是否定 LLM，而是否定"LLM 持有文件工具"的架构。 | 适用于涉及 AI 与程序职责边界、事故驱动决策、外部依赖选型的场景。用户的判断模式：事故后追问"根因是架构还是实现"，如果是架构则重新划分职责而非补丁。用户对 LLM 的态度是"可用但需约束"——让 LLM 做内容建议，程序做执行决策。 | diff3 算法自研（约 150 行代码）有可靠性风险，用户接受这个风险但要求"以 git merge 为对照测试"。如果未来发现自研算法在实际使用中有边界 bug，需要重新评估是否引入 merge3 包或切换到完整 git-like 方案。前提 6（自研够用）是用户基于"主备模式下 per-file hash 已足够"的判断，如果未来需要多端同步，这个前提失效，需切换到方案 C（完整 git-like）。 |
| 2026-07-17 | [数据备份策略：平铺存储 + 复用调度器 + 不做恢复 API](.\2026-07-17-data-backup-strategy.md) | 用户在数据备份策略上展现了"用户视角驱动技术选型"的判断模式：(1) 否决 zip 打包的核心理由是"查看备份内容时不方便"——从用户操作便利性反推技术方案，而非从压缩率等技术指标出发；(2) 否决独立 asyncio 调度器的理由是"项目已有 ScheduleService"——避免重复造轮子，但也明确表示"当前项目是有 scheduler"而非凭记忆判断，要求 AI 调研现有代码；(3) 在恢复 API 决策上明确判断"恢复场景频率不高，做 API 太复杂，先做好备份就行"——投入产出比驱动的决策，不为低频场景过度设计。用户明确表达"当前主要面向对象还是我自己"——这个前提是决策的关键基础，如果未来面向终端用户需重新评估。 | 适用于涉及备份策略、调度器选型、恢复 API 必要性评估的场景。用户对"查看便利性"的优先级高于"压缩节省空间"，这反映了对"备份是给人看的"的核心理解。用户对"不做恢复 API"的决策基于明确的频率判断（年频），而非"以后再做"的拖延。 | "不做恢复 API"前提 3（用户是开发者，可手工恢复）在未来面向终端用户时失效。用户已主动标注"未来可能会做"——这是显式的备选触发条件。如果未来面向终端用户，需基于恢复文档扩展为 API + Agent 通道。前提 6（数据量小，无压缩需求）在数据量增长到 GB 级时失效，需重新评估 git-like 方案。 |
| 2026-07-17 | [冲突失败处理策略：不阻塞 + 不主动通知](.\2026-07-17-conflict-failure-policy.md) | 用户在冲突失败处理上展现了"决策一致性驱动"的判断模式：(1) 选择"不主动通知用户"的核心逻辑是"与不做 Agent 恢复通道的决策一致"——不为单一场景打破整体架构决策；(2) 主动识别 sync_conflict/ 只备份本地的 bug——"本地它做那个冲突文本备份，但是它只备份了本地的，它没有备份云端的，云端的同样需要备份"，这个识别基于对现有代码的理解而非理论分析；(3) 明确接受"用户被动发现冲突"的风险，理由是"当前主要面向对象还是我自己"——与 ADR-2 数据备份策略的前提一致，决策具有内部一致性。 | 适用于涉及失败处理策略、用户通知机制的场景。用户的判断模式：在已有架构决策约束下做配套决策，避免引入与整体决策矛盾的新机制。用户对 bug 的识别敏锐——能从"备份完整性"角度发现 sync_conflict/ 只备份本地的缺陷。 | 前提 3（用户是开发者，可接受被动发现）在未来面向终端用户时失效，需切换到方案 B（Agent 通知）。前提 5（与不做 Agent 恢复通道决策一致）如果未来扩展了 Agent 通道，本决策需重新评估。sync_conflict/ 同时备份本地和云端的修复是 bug fix 性质，不是新决策，但用户主动识别这个 bug 体现了对"用户可恢复性"的关注。 |
| 2026-07-17 | [备份范围与同步范围解耦：职责不同](.\2026-07-17-backup-sync-decoupled-scope.md) | 用户在备份范围决策上展现了"职责驱动的解耦"判断模式：(1) 否决复用 SYNC_DIRECTORIES 的核心理由是"同步和备份的职责本来就不同——同步是从云端和本地的功能性考虑，备份是从数据安全性考虑"——从职责出发而非从代码复用出发；(2) plan 不加入同步的判断基于功能性推理："Agent 无法读取 plan 文件夹，而且 plan 文件夹和数据库高度绑定，一旦修改错误很难恢复"——从"是否有功能性需求"+"数据特性"两个维度评估；(3) 用户主动扩展了讨论范围——不只是回答"plan 是否备份"，而是从 plan 引申出"同步和备份的职责差异"，这种从具体到抽象的思考方式是典型的架构师判断。 | 适用于涉及职责分离、白名单/黑名单设计、目录范围定义的场景。用户的判断模式：先定义职责（同步=功能性，备份=安全性），再基于职责决定范围。不为"单一数据源"原则强行耦合职责不同的系统。 | 前提 3（plan 无同步必要，Agent 无法读取）如果未来 Agent 扩展了 plan 读取能力则失效，需重新评估。前提 6（sync_client 不稳定，不引入新变量）是当前事实状态，如果 sync_client 重构后稳定则失效。前提 7（避免不必要耦合）是用户偏好，不易失效。纪律性约束（BACKUP_DIRS 与 SYNC_DIRECTORIES 内容一致性）依赖人工维护，未来可能脱节。 |

## 决策偏好总结

- 在数据安全与性能之间，优先选择数据安全的方案，即使代价是重复操作
- 评估策略时关注"失败后能否恢复"，而非仅看正常流程的表现
- 对"标准实践"不执着，优先选择最简单可行的方案，但会主动暴露知识盲区并请求确认
- 从实际使用场景出发评估技术方案的必要性，不追求理论上的完备性
- 对不可恢复资源（密钥、token）采取防御性策略，倾向于保留而非删除
- **按字段职责做时间策略分化**：不做"所有时间都转 UTC"的一刀切，而是区分系统级字段（created_at/updated_at/event_time，需要 UTC 统一和查询支持）和用户数据字段（自定义字段，只做展示视为普通字符串）。当 Agent 能提供合理默认值时，倾向必填约束而非可选
- **务实优先于功能完备性**：当当前需求能被简单方案完全覆盖时，不引入复杂方案，即使复杂方案在功能上更完备。判断依据是"当前需求是否完全覆盖"，而非"方案本身是否合理"
- **静默数据丢失 > 依赖用户记忆**：在风险权衡中，不可恢复的静默数据丢失比可缓解的"依赖用户记忆"风险更严重。优先选择不会导致数据丢失的方案，即使代价是用户需要手动操作
- **任务派发基于"谁最理解数据"**：当决定"谁来做合并/处理"时，判断依据不是"谁在看"而是"谁产生了数据"。数据是 AI 生成的就交给 AI，是用户生成的就保留给用户手动介入。这个原则在 v2.0 中驱动了 MD 冲突处理从手动到 AI 自动的转变
- **方向性错误优先于表面修复**：当发现 bug 时，先追问"检测的方向和实际操作的方向是否一致"，不满足于修复表面现象。如果根因是方向错误，即使最小改动方案能治标也不接受，坚持从根因入手
- **决策范围严格控制**：识别到衍生问题时，明确判断"是否在当前决策范围内"。如果衍生问题需要独立决策，标注为"需要独立决策"并记录到后续影响，而非顺手在当前决策中处理，避免决策范围蔓延
- **事故驱动决策——根因是架构还是实现**：当生产事故发生后，先追问根因是架构问题还是实现问题。如果是架构问题（如 LLM 持有文件工具导致截断），则重新划分职责而非补丁；如果是实现问题，则修复实现。不为架构问题做工程补丁，因为补丁无法消除架构性风险
- **用户视角驱动技术选型**：在备份格式、UI 形式等技术选型上，从用户操作便利性反推技术方案，而非从技术指标（压缩率、API 完备性）出发。备份是给人看的，查看便利性 > 压缩节省空间
- **投入产出比驱动功能必要性**：为低频场景（年频恢复）做 API/UI 投入产出比低。用户明确判断"频率不高，做太复杂，先做好基础就行"——不为低频场景过度设计，但保留显式的"未来可能做"备选触发条件
- **决策一致性驱动配套决策**：在已有架构决策约束下做配套决策时，避免引入与整体决策矛盾的新机制。例如"不做 Agent 恢复通道"决策后，冲突失败通知也走"不主动通知"路径，不为单一场景打破整体架构
- **职责驱动的解耦**：当两个系统职责不同时（如同步=功能性、备份=安全性），即使内容相似也不强行耦合。不为"单一数据源"原则牺牲职责清晰度。先定义职责，再基于职责决定范围

## 给后续 agent 的参考

- 设计同步、重试、增量查询相关功能时，必须验证"失败场景下数据是否可恢复"
- 不要仅因为"正常流程性能更好"就选择更细粒度的策略，需先确认失败场景下的数据安全
- 涉及"标准实践 vs 简单方案"时，主动补充技术对比，不要假设用户了解标准实践的优势
- 用户从使用场景倒推技术方案时，帮助标注"使用模式变化后的重新评估条件"
- 涉及不可恢复资源时，默认采取保留策略，而非删除
- **涉及时间处理时，先分析字段的职责定位**——是系统基础设施（created_at 等）还是用户输入数据（自定义字段）。系统级字段做 UTC 转换，用户字段不转换。当 Agent 能提供合理默认值时，推荐必填约束。自定义字段"不转换"的前提是"不用于查询"，新增查询功能时需主动检查此前提是否仍然成立
- **用户提出"都做"时主动评估成本**——当用户初始倾向"都做"多个方案时，应主动评估每个方案的修改量、对当前逻辑的损坏、可能引起的 bug，帮助用户做务实判断。用户会在具体成本评估后收回"都做"的倾向
- **风险权衡中优先避免静默数据丢失**——当方案 A 可能导致静默数据丢失、方案 B 依赖用户记忆但可缓解时，优先选择方案 B。不可恢复的风险比可缓解的风险更严重
- **任务派发先问"数据是谁产生的"**——当设计方案需要"谁来做某件事"（合并、处理、审核）时，先判断数据是谁生成的。AI 生成的数据交给 AI 处理，用户生成的数据保留用户介入。不要默认"用户应该看最终结果"或"AI 可以自动处理一切"。
- **识别 bug 时先检查方向性**——当发现触发条件、对比逻辑、检测机制类问题时，先追问"检测的方向和实际操作的方向是否一致"，不要直接接受最小改动方案。方向错误必须从根因修复，即使成本更高
- **遇到衍生问题不扩大当前决策范围**——当决策过程中识别到衍生问题（如并发锁、id 冲突等）时，明确判断是否在当前范围内。不在范围内的标注为"需要独立决策"并记录到后续影响，而非顺手处理
